# LunarVim configuration

Clone this repo to '~/.config/lvim'
```git clone git@github.com:UmbraAdMortem/lvim.git ~/.local/lvim```

Execute install script 'install.sh' only once
```~/.config/lvim/install.sh```

